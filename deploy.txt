Deployment Handoff

Git Repo
- Current origin: https://github.com/nirajk009/oxam_scapepr.git
- Main branch: main
- Local repo path: D:\scrper

SSH Access
- Host/IP: 162.241.123.162
- User: golde5qk
- Private key path: D:\testing\key\id_rsa
- Key passphrase: M1PO+YEE6oqc
- Primary SSH command:
  ssh -i D:\testing\key\id_rsa golde5qk@162.241.123.162
- If "Too many authentication failures":
  ssh -o IdentitiesOnly=yes -i D:\testing\key\id_rsa golde5qk@162.241.123.162

Hosting Paths
- Home: /home4/golde5qk
- Live app URL: https://aplusap.goldengatetechnolabs.com/
- Expected live app path: /home4/golde5qk/aplusap.goldengatetechnolabs.com
- Do not touch server .htaccess
- Preserve hosting .env when deploying

Hosting App Notes
- This project deploys from the repo root.
- Laravel web entrypoint is public/index.php.
- Local Windows success uses OXAAM_TRANSPORT=curl_binary with curl.exe.
- Shared hosting is working with the official curl-impersonate Linux binary, not the stock host curl and not the native Laravel HTTP client.
- Working hosting binary path:
  /home4/golde5qk/oxaam-tools/curl-impersonate-v0.6.1/curl_chrome116
- Stock host curl and native PHP/Node HTTP clients were blocked by Cloudflare on this server.
- Browser-triggered scraping is configured to run directly on hosting.
- Production monitor compares each new batch against the last emailed snapshot, not merely the immediately previous batch.

Suggested Hosting .env Direction
- Keep the existing hosting .env file.
- Set APP_ENV=production
- Set APP_DEBUG=false
- Set APP_URL=https://aplusap.goldengatetechnolabs.com
- Set the hosting DB credentials already created for this app
- Set MAIL_MAILER=smtp
- Set MAIL_SCHEME=smtp
- Set MAIL_HOST=smtp.gmail.com
- Set MAIL_PORT=587
- Set MAIL_USERNAME to the Gmail sender account
- Set MAIL_PASSWORD to the Gmail app password
- Set MAIL_FROM_ADDRESS to the Gmail sender account
- Set MAIL_FROM_NAME=Oxaam Scraper
- Set OXAAM_TRANSPORT=curl_binary
- Set OXAAM_CURL_BINARY=/home4/golde5qk/oxaam-tools/curl-impersonate-v0.6.1/curl_chrome116
- Set OXAAM_VERIFY_SSL=false
- Set OXAAM_WEB_TRIGGER=direct

Deploy Steps
1. First deploy:
   cd /home4/golde5qk
   git clone https://github.com/nirajk009/oxam_scapepr.git aplusap.goldengatetechnolabs.com
2. Existing deploy:
   cd /home4/golde5qk/aplusap.goldengatetechnolabs.com
   git pull origin main
3. Install/update PHP deps:
   composer install --no-dev --optimize-autoloader
4. Laravel upkeep:
   php artisan migrate --force
   php artisan config:clear
   php artisan cache:clear
   php artisan route:clear
   php artisan view:clear

Useful Runtime Commands
- One scrape:
  php artisan oxaam:scrape --runs=1
- Batch scrape with delay:
  php artisan oxaam:scrape --runs=25 --sleep=1200
- Production monitor command:
  php artisan oxaam:monitor --runs=150 --mode=changed --profile=production --to=niraj.edigillence@gmail.com
- Test monitor command:
  php artisan oxaam:monitor --runs=150 --mode=always --profile=test --to=niraj.edigillence@gmail.com --subject-prefix=TEST
- Production cron target:
  */30 * * * * cd /home4/golde5qk/aplusap.goldengatetechnolabs.com && /usr/local/bin/php artisan oxaam:monitor --runs=150 --mode=changed --profile=production --to=niraj.edigillence@gmail.com >> /home4/golde5qk/aplusap.goldengatetechnolabs.com/storage/logs/oxaam-monitor.log 2>&1
- Test cron target:
  * * * * * cd /home4/golde5qk/aplusap.goldengatetechnolabs.com && /usr/local/bin/php artisan oxaam:monitor --runs=150 --mode=always --profile=test --to=niraj.edigillence@gmail.com --subject-prefix=TEST >> /home4/golde5qk/aplusap.goldengatetechnolabs.com/storage/logs/oxaam-monitor-test.log 2>&1

Important Rules
- Do not install anything globally
- Do not touch server .htaccess
- Do not overwrite hosting .env
- Verify the hosting curl-impersonate binary still exists after deploy or account cleanup
